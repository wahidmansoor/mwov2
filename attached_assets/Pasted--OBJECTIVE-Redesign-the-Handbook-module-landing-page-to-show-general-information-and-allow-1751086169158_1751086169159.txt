// 🧠 OBJECTIVE:
// Redesign the Handbook module landing page to show general information and allow users to choose between:
  // → Medical Oncology Handbook
  // → Radiation Oncology Handbook
  // → Palliative Care Handbook

// 🎨 DESIGN REQUIREMENTS:
1. Create a modern, elegant landing page for the HandbookModule
2. Include:
   - A welcome message and description of the module
   - Three category cards or buttons for selection:
     - Medical Oncology Handbook
     - Radiation Oncology Handbook
     - Palliative Care Handbook
   - Each card routes to a dedicated view for that category's content

// 🧩 FILES TO CREATE / MODIFY:
src/modules/handbook/
├── HandbookModule.tsx          ← New: Landing page with categories
├── MedicalOncologyView.tsx     ← Will contain the Medical Oncology index and viewer
├── RadiationOncologyView.tsx   ← Will contain the Radiation Oncology index and viewer
├── PalliativeCareView.tsx      ← Will contain the Palliative Care index and viewer
├── components/
│   ├── HandbookCategoryCard.tsx ← UI card for each handbook category
│   ├── TOCGenerator.tsx         ← Receives pasted index & renders nested ToC
│   └── MarkdownViewer.tsx       ← Reusable MD file renderer

// ⚙️ FUNCTIONALITY:
- When the user visits `/handbook`, show the landing page
- After selecting a category, route to `/handbook/medical`, `/handbook/radiation`, or `/handbook/palliative`
- In each category view:
  1. Show the parsed table of contents (TOC) for that section only
  2. Allow selecting chapters/subsections that load markdown in MarkdownViewer

// 🧾 THE INDEX WILL BE PASTED AS TEXT INTO EACH VIEW:
// Each handbook category will load its TOC from a pasted index structure under:
// - MedicalOncologyView.tsx
- RadiationOncologyView.tsx
- PalliativeCareView.tsx

// ✨ UI STYLE GUIDE:
- Use Tailwind CSS
- Add dark mode support
- Keep consistency with OncoVista app style
- Include optional hero icons for each handbook category (e.g., DNA, radiation, compassion)

